"""``specs`` -- the declarative spec schema package.

Lives at the repo root (sibling of ``src/macrotoolkit`` and ``stan/``) per
``lw-sv-spec.md`` §6's repository layout: the spec schema is the spine of the
toolkit and is deliberately not buried inside the ``macrotoolkit`` Python
package.
"""
